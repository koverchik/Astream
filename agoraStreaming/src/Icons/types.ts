export type CameraMutePropsType = {
  fill?: string;
  size?: string;
};

export type TabIconPropsType = {
  color: string;
  size: number | string;
};

export type AvatarPropsType = {
  size: number;
};
